name: CI
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:
jobs:
  checkout: # Checks-out repository under $GITHUB_WORKSPACE
    name: Checkout repository
    runs-on: ubuntu-latest
    steps: 
    - uses: actions/checkout@v2

  install_node:
    name: Install node
    runs-on: ubuntu-latest
    steps:
    - name: node
      uses: actions/setup-node@v2
      with:
        node-version: '16'
        cache: 'npm'

  install_gcloud:
    name: Install gcloud CLI
    runs-on: ubuntu-latest
    steps:
    - uses: google-github-actions/setup-gcloud@master
      with:
        project_id: ${{ secrets.GCP_PROJECT_ID }}
        service_account_key: ${{ secrets.GCP_SA_KEY }}
        export_default_credentials: true

  build:
    name: Build project
    runs-on: ubuntu-latest
    needs: [checkout, install_node]
    steps:
    - name: Install dependencies
      run: npm ci
    - name: Compile typescript
      run: |
        cd $GITHUB_WORKSPACE
        ./node_modules/.bin/tsc -p tsconfig.json
        cp app.yaml /build

  deploy:
    name: Deploy App Engine
    runs-on: ubuntu-latest
    needs: [build, install_gcloud]
    steps:
      - name: Deploy glcoud app engine
        run: |
          cd $GITHUB_WORKSPACE/build
          gcloud app deploy
